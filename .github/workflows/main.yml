name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build
      run: |
        pwd
        ls -l
        docker pull raz0red/devkitppc
        docker run -v $(pwd)/wiicolem:/source raz0red/devkitppc bash -c 'cd /source; make; ls -l /source'